<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= title %> </title>
    <link rel="stylesheet" href="/styles.css" >
</head>
<body>
    <header>
        <h1>Movie List Site</h1>
        <nav>
            <ul>
                <li><a href="/movies">Movie List</a></li>
                <% if (currentUser && currentUser.type === 'admin') { %>
                <li><a href="/movies/add">Add Movie</a></li>
                <% } %>
            </ul>
        </nav>
        <div id="userInfo">
            <% if (currentUser) { %>
                <p>Logged in as: <%= currentUser.username %> (<%= currentUser.type %>)</p>
                <form action="/logout" method="POST" style="display:inline;">
                    <button type="submit">Logout</button>
                </form>
            <% } else { %>
                <a href="/">Login</a> | <a href="/register">Register</a>
            <% } %>
        </div>
    </header>